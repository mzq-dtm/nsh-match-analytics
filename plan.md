# 联赛团队战报长图导出实施计划

## 1. 目标与范围

在 `MatchRecords.vue` 的联赛操作区增加“导出”按钮。用户选择某场联赛后按钮立即可用；点击按钮后，组件把已经加载并用于页面展示的完整本帮数据交给图片生成工具，生成一张包含所有团队和全部规定表格的高清 PNG 长图，并直接触发浏览器下载。

本次实现遵循以下边界：

- 只改前端，不修改 Flask API、数据库 schema 或导入协议。
- `MatchRecords.vue` 继续负责现有的比赛数据读取和页面状态，并把当前比赛的 `homePerformances`、比赛名称、胜负和备注传给图片生成工具。
- 报告建模和 Canvas 绘制全部放在 `frontend/src/features/match-report/`；该功能目录不重复请求后端 API，也不负责触发浏览器下载。
- 按钮绑定函数保留在 `MatchRecords.vue`，核心职责只有两项：调用图片生成工具，以及使用返回的 Blob 触发下载。

## 2. 用户操作流程

1. 用户进入“联赛数据”页面。
2. 未选择比赛时，“导出”按钮保持禁用。
3. 用户选择一场比赛后，按钮变为可点击。
4. 用户点击按钮，按钮文字切换为“生成中”，并在生成期间禁用，避免重复操作。
5. `MatchRecords.vue` 将当前已经加载的完整本帮数据、比赛名称、胜负和备注传给图片生成工具。
6. 图片生成工具按既定规则分团、过滤、排序并绘制长图。
7. 图片生成工具返回 PNG Blob 和建议文件名后，按钮绑定函数立即触发浏览器下载，不额外打开预览页面。
8. 生成失败时不下载残缺文件，在联赛操作区显示可读错误信息，并恢复按钮状态。

导出过程不得改变用户当前的本帮/对手视图、子标签、表格排序、列显隐或高亮状态。即使用户当前正在查看对手数据，导出的也始终是所选比赛的本帮数据。

## 3. 报告数据规则

### 3.1 团队划分与顺序

- 使用 `leader_nick` 对本帮参赛记录分团。
- 正常团队按照团长昵称的中文友好字典序排列。
- 排序使用 `zh-CN` 本地化比较并启用数字自然排序，例如“团长2”排在“团长10”之前。
- 如果本地化比较结果相同，再使用原始昵称进行稳定兜底比较，保证每次导出顺序一致。
- `leader_nick` 为“无”、空值或缺失时，统一归入“未分团”。
- “未分团”始终放在所有正常团队之后，确保数据不被遗漏。
- 长图中的团队标题显示团队序号和团长昵称，例如“第1团｜团长：某某｜参赛人数：30”。

### 3.2 每个团队的四张表

每个团队必须连续生成四张完整数据表，顺序固定如下：

1. **对玩家伤害表**
   - 排除所有 `profession_name === '素问'` 的记录。
   - 按 `damage_to_players` 降序排列。

2. **对建筑伤害表**
   - 排除所有 `profession_name === '素问'` 的记录。
   - 按 `damage_to_structures` 降序排列。

3. **击杀数表**
   - 排除所有 `profession_name === '素问'` 的记录。
   - 按 `kills` 降序排列。

4. **治疗量表**
   - 只保留 `profession_name === '素问'` 的记录。
   - 按 `healing_amount` 降序排列。

相同排序字段值使用昵称进行稳定排序。表格“序号”按照最终显示顺序从 `1` 连续编号，不把它改造成并列名次字段。

如果某个团队没有素问，仍然绘制治疗表标题、表头和空状态，并显示“本团无素问参赛数据”，以保持所有团队的报告结构一致。

### 3.3 固定公开字段

导出图使用固定字段集合，不读取用户当前的列显隐配置，避免不同管理员导出的战报内容不一致。首版字段顺序沿用联赛页面默认公开字段：

1. 序号
2. 昵称
3. 职业
4. 所在团长
5. 总战力
6. KD
7. 总伤害
8. 击败
9. 助攻
10. 对玩家伤害
11. 对建筑伤害
12. 承受伤害
13. 重伤
14. 治疗值
15. 化羽
16. 青灯焚骨
17. 控制
18. 战备资源

默认隐藏的玩家 ID、等级、装备评分、技能评分、修炼和单次击杀所用伤害不进入公开长图。

所有数值采用统一格式：整数使用千位分隔，KD 保留两位小数，空值按当前前端的规范化规则显示为 `0` 或明确的空状态。

## 4. 长图内容与布局

整张图片从上到下依次绘制：

```text
比赛总标题、对阵双方、比赛时间、胜负、备注

字典序第一个团队标题
  对玩家伤害表
  对建筑伤害表
  击杀数表
  治疗量表

字典序第二个团队标题
  对玩家伤害表
  对建筑伤害表
  击杀数表
  治疗量表

……

未分团（如存在）
  四张表
```

布局要求：

- 图片使用固定高清宽度，保证每次导出的列宽和字号一致，不受浏览器窗口大小影响。
- 图片高度根据团队数量、各表行数、空状态、备注换行和间距动态计算。
- 全局标题与数据表之间、不同团队之间使用明显的横幅、留白和分隔线。
- 每张表重复绘制表名和列标题，避免阅读长图中段时失去字段上下文。
- 表格使用固定列宽；昵称和备注超出可用宽度时按既定规则截断或换行，不能挤压其他数据列。
- 职业单元格颜色沿用现有职业配色，并继续使用对比色保证文字可读。
- 使用交替行底色和清晰网格线，保持长图中大量数据的可读性。

## 5. 数据条和排序字段强调

### 5.1 数据条

所有当前支持数据条的统计字段都要保留可视化效果：

- 伤害、击杀、KD等使用现有红色系。
- 治疗、化羽使用现有绿色系。
- 青灯、控制、战备资源使用现有蓝色系。
- 承受伤害、重伤使用现有黄色系。

每张表独立计算各数据字段的最大值：

```text
数据条宽度 = 单元格可绘制宽度 × 当前数值 ÷ 本表该字段最大值
```

- 本表最大值对应 `100%` 长度。
- 最大值为 `0`、值为空或值小于等于 `0` 时不绘制数据条。
- 数据条先于文字绘制，使用半透明颜色，数值始终位于数据条上方。
- 每张表下方标注“数据条按本表各字段最大值缩放”，避免用户把不同表格中的条形长度误认为统一量程。

颜色定义和字段分类应复用 `frontend/src/features/match-records/constants.ts`，避免网页和导出图产生两套含义不同的颜色规则。

### 5.2 排序字段外框

每张表必须明确框出当前排序字段：

- 表名明确写出“按某字段降序”。
- 排序字段表头增加 `↓`。
- 排序字段表头使用更深的背景色。
- 在整张表绘制完成后，从该列表头顶部到最后一行底部绘制连续的粗边框。
- 外框应包围整列，而不是给每个单元格分别加粗，避免视觉断裂。
- 排序列的数据条可以适度加深，但必须保证数值文字清晰。

四张表分别框出：

- `damage_to_players`
- `damage_to_structures`
- `kills`
- `healing_amount`

## 6. 功能模块组织

新增目录：

```text
frontend/src/features/match-report/
  types.ts
  constants.ts
  buildMatchReport.ts
  generateMatchReportImage.ts
```

### 6.1 `types.ts`

定义功能内部的数据契约，包括：

- 比赛标题信息；
- 导出列定义；
- 团队报告模型；
- 四类表格模型；
- 已排序的表格行；
- 各字段数据条最大值；
- Canvas 布局测量结果；
- 图片生成结果。

### 6.2 `constants.ts`

保存固定功能配置：

- 四张表的顺序、过滤规则说明、排序字段和标题；
- 固定公开列及列宽；
- 字号、行高、边距、团队间距和表格间距；
- 排序列边框样式；
- 文件名后缀和图片背景色。

数据条颜色优先从现有 match-records 常量复用，不复制一套新的颜色值。

### 6.3 `buildMatchReport.ts`

作为纯数据处理模块，负责：

- 接收并校验 `MatchRecords.vue` 传入的完整 `homePerformances`；
- 按团长分组；
- 按团长昵称排列团队；
- 把“无”或缺失团长放到最后；
- 为每个团队建立四张表；
- 执行素问/非素问过滤；
- 按目标字段降序和昵称稳定排序；
- 生成连续序号；
- 计算每张表各数据条字段的最大值；
- 保证处理过程不修改组件传入的原数组或行对象。

该文件不访问 API、不使用 Vue、不创建 Canvas，也不触发下载，以便独立测试。

### 6.4 `generateMatchReportImage.ts`

这是 `MatchRecords.vue` 调用的唯一图片生成入口。它接收普通值，不读取 Vue ref，也不访问组件内部状态：

```ts
generateMatchReportImage({
  matchId,
  matchName,
  homePerformances,
  homeOutcome,
  note,
})
```

该入口先调用 `buildMatchReport` 建立报告模型，再集中完成全部 Canvas 绘制，最后返回：

```ts
Promise<{
  blob: Blob
  filename: string
  width: number
  height: number
}>
```

绘制分为两遍：

1. 测量全局标题、备注、团队标题、表头、数据行、空状态和间距，得到最终宽高。
2. 创建 Canvas，并按报告模型顺序绘制完整长图。

该文件负责：

- 等待 `document.fonts.ready`；
- 绘制全局标题和比赛信息；
- 绘制团队横幅；
- 绘制表名、表头、数据行和空状态；
- 绘制职业颜色；
- 绘制数据条；
- 绘制排序字段整列外框；
- 绘制数据条量程说明；
- 检查最终图片尺寸；
- 使用 `canvas.toBlob('image/png')` 返回 PNG Blob。

绘图模块不得调用比赛 API、读取 Vue ref、修改页面状态、自行寻找 DOM 表格或触发浏览器下载。它只根据传入数据生成图片结果。

## 7. `MatchRecords.vue` 的最小改动

`MatchRecords.vue` 中不加入任何分团、过滤、排序、数据条计算或 Canvas 绘图逻辑。组件继续持有并使用现有的 `homePerformances`、`matches`、`matchOutcome` 和 `matchNote`，不为导出功能重复请求数据。

按钮绑定函数的核心流程严格保持为：

1. 把当前组件数据传给 `generateMatchReportImage`。
2. 使用返回的 Blob 和文件名触发浏览器下载。

生成中状态、错误捕获和临时 Object URL 清理属于这两步操作的必要交互与资源管理，不承载报告业务规则。

仅进行以下改动：

1. 导入 `generateMatchReportImage`。
2. 增加 `isGeneratingReport` 和 `reportError` 两个简单交互状态。
3. 在现有操作区增加一个按钮。
4. 将按钮的禁用状态绑定到“未选择比赛”或“正在生成”。
5. 增加一个小型按钮处理函数，把当前 `homePerformances` 和比赛元数据传入图片工具，并触发 Blob 下载。
6. 在操作区显示 `reportError`。
7. 尽量复用现有按钮样式；只有确有需要时增加少量样式。

模板交互目标如下：

```vue
<button
  :disabled="!selectedMatch || isGeneratingReport"
  @click="downloadTeamReport"
>
  {{ isGeneratingReport ? '正在生成……' : '导出团队长图' }}
</button>
```

按钮处理函数从 `matches` 中找到当前比赛名称，并直接使用当前已有的 `homePerformances`、`matchOutcome` 和 `matchNote`。它不得重新调用 `getMatches`、`getPerformances` 或 `getMatchResult`。

除上述接线和下载 Blob 所需的少量代码外，不重构 `MatchRecords.vue` 的既有加载、缓存、筛选、排序或弹窗逻辑。

## 8. 错误与边界处理

功能模块需要覆盖：

- 比赛 ID 无效；
- 比赛不存在；
- 组件传入的比赛名称不存在；
- 组件传入的本帮数据尚未准备好或为空；
- 组件传入的数据不属于当前选择的比赛；
- 比赛结果或备注不存在；
- 职业、团长或数值字段为空；
- 所有玩家均为素问或均为非素问；
- 某个团队没有素问；
- 团长昵称、玩家昵称或备注过长；
- `canvas.getContext('2d')` 失败；
- Canvas 最终尺寸超过预设安全上限；
- `canvas.toBlob()` 返回空值；
- 用户在生成期间重复点击按钮。

任何失败都不得下载空文件或截断图片；错误信息应回到 `MatchRecords.vue` 的操作区显示。

## 9. 测试与验证

### 9.1 纯数据逻辑测试

为 `buildMatchReport.ts` 覆盖：

- 团长昵称中文字典序；
- 昵称数字自然排序；
- 相同团长排序结果的稳定兜底；
- “未分团”始终位于最后；
- 前三张表完全排除素问；
- 治疗表只包含素问；
- 四张表的顺序和排序字段正确；
- 相同指标值按昵称稳定排列；
- 空治疗表模型正确；
- 数据条最大值按当前表计算；
- 输入数组和输入行对象不被修改。

### 9.2 功能验收数据

准备一份覆盖以下情况的固定测试数据：

- 至少四个正常团队；
- 每团同时包含素问和非素问；
- 一个没有素问的团队；
- 一组未分团玩家；
- 相同伤害、相同击杀或相同治疗值；
- 数字团长昵称；
- 长玩家昵称；
- `0` 值和空值；
- 长备注。

### 9.3 最终验收标准

- 未选择比赛时按钮不可点击。
- 选择比赛后按钮可点击。
- 点击一次最终只下载一张 PNG。
- 生成期间按钮不可重复点击。
- 四个正常团队时，图片包含十六张表。
- 团队顺序符合团长昵称字典序，未分团位于最后。
- 每个团队的四张表顺序完全符合约定。
- 前三张表没有素问，第四张表没有其他职业。
- 所有表格排序结果正确且稳定。
- 固定公开字段没有遗漏、错列或受页面显隐影响。
- 数据条颜色、长度和数值正确。
- 每张表的目标排序列被完整粗框包围。
- 治疗空表、长昵称、大数值和长备注不会破坏布局。
- 用户当前查看对手数据时，导出内容仍然是本帮数据。
- 导出操作不改变页面现有状态。
- 图片生成过程不额外调用任何比赛 API。
- `MatchRecords.vue` 中不存在报告业务计算或 Canvas 绘图代码，下载触发逻辑保留在按钮处理函数中。

完成后执行现有质量检查：

```bash
cd frontend
npm run lint
npm run type-check
npm run build
```

## 10. 明确不在本次范围内的内容

- 管理员导入成功页增加导出入口；
- 自动向微信、QQ或其他群聊发送图片；
- 服务端生成、保存或归档图片；
- 手工调整团队顺序；
- 自定义导出列、颜色、字体和布局；
- 图片预览、编辑、裁剪或水印配置；
- 多张图片或 ZIP 下载；
- 数据库字段和 API 契约调整。
